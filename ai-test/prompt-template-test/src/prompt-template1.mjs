import "dotenv/config";
import { ChatOpenAI } from "@langchain/openai";
import { PromptTemplate } from "@langchain/core/prompts";

const model = new ChatOpenAI({
  modelName: process.env.MODEL_NAME,
  apiKey: process.env.OPENAI_API_KEY,
  temperature: 0,
  configuration: {
    baseURL: process.env.OPENAI_BASE_URL,
  },
});

const naiveTemplate = PromptTemplate.fromTemplate(`
你是一名严谨但不失人情味的工程团队负责人，需要根据本周数据写一份周报。

公司名称：{company_name}
部门名称：{team_name}
直接汇报对象：{manager_name}
本周时间范围：{week_range}

本周团队核心目标：
{team_goal}

本周开发数据（Git 提交 / Jira 任务）：
{dev_activities}

请根据以上信息生成一份【Markdown 周报】，要求：
- 有简短的整体 summary（两三句话）
- 有按模块/项目拆分的小结
- 用一个 Markdown 表格列出关键指标（字段示例：模块 / 亮点 / 风险 / 下周计划）
- 语气专业但有一点人情味，适合作为给老板和团队抄送的周报。
`);

const prompt = await naiveTemplate.format({
  company_name: "星航科技",
  team_name: "数据智能平台组",
  manager_name: "刘总",
  week_range: "2025-03-10 ~ 2025-03-16",
  team_goal: "完成用户画像服务的灰度上线，并验证核心指标是否达标。",
  dev_activities:
    "- 阿兵：完成用户画像服务的 Canary 发布与回滚脚本优化，提交 27 次，相关任务：DATA-321 / DATA-335\n" +
    "- 小李：接入埋点数据，打通埋点 → Kafka → DWD → 画像服务的全链路，提交 22 次\n" +
    "- 小赵：完善画像服务的告警与Dashboard，新增 8 个告警规则，提交 15 次\n" +
    "- 小周：配合产品输出 A/B 实验报表，支持 3 条对外汇报用数据",
});

console.log("格式化后的提示词:");
console.log(prompt);

const stream = await model.stream(prompt);
console.log("\nAI 回答:");
for await (const chunk of stream) {
  process.stdout.write(chunk.content);
}

/**
 * 格式化后的提示词:

你是一名严谨但不失人情味的工程团队负责人，需要根据本周数据写一份周报。

公司名称：星航科技
部门名称：数据智能平台组
直接汇报对象：刘总
本周时间范围：2025-03-10 ~ 2025-03-16

本周团队核心目标：
完成用户画像服务的灰度上线，并验证核心指标是否达标。

本周开发数据（Git 提交 / Jira 任务）：
- 阿兵：完成用户画像服务的 Canary 发布与回滚脚本优化，提交 27 次，相关任务：DATA-321 / DATA-335
- 小李：接入埋点数据，打通埋点 → Kafka → DWD → 画像服务的全链路，提交 22 次
- 小赵：完善画像服务的告警与Dashboard，新增 8 个告警规则，提交 15 次
- 小周：配合产品输出 A/B 实验报表，支持 3 条对外汇报用数据

请根据以上信息生成一份【Markdown 周报】，要求：
- 有简短的整体 summary（两三句话）
- 有按模块/项目拆分的小结
- 用一个 Markdown 表格列出关键指标（字段示例：模块 / 亮点 / 风险 / 下周计划）
- 语气专业但有一点人情味，适合作为给老板和团队抄送的周报。


AI 回答:
```markdown
# 🌟 星航科技 · 数据智能平台组周报  
**汇报周期**：2025-03-10 至 2025-03-16  
**呈报对象**：刘总  
**抄送**：全体数据智能平台组成员  

---

## 📌 整体小结  
本周我们稳稳踩在“用户画像服务灰度上线”这条主线上——所有同学都像齿轮一样咬合得刚刚好：链路通了、脚本稳了、监控亮了、报表也交到了产品手上。核心指标全部达标，灰度期间服务 P99 延迟 < 320ms，错误率稳定在 0.017%，用户标签覆盖率提升至 98.4%（较上周 +5.2pct）。特别想说一句：阿兵凌晨两点还在调通回滚验证，小李为对齐埋点语义和客户端拉了三轮对齐会……这些“看不见的工时”，是系统真正可靠的底气 💫

---

## 🧩 模块进展小结  

### ✅ 用户画像服务灰度上线（核心项目）  
- 已于 3月14日（周四）完成 Canary 发布，覆盖 8% 生产流量（含新老用户混合样本），持续观察 48 小时无异常；  
- 全链路压测通过（QPS 1200+，CPU 峰值 63%，内存水位平稳），自动回滚脚本经 3 轮模拟演练验证有效；  
- 灰度期间收到 2 条轻量级反馈（标签更新延迟感知、部分设备 ID 归因模糊），已纳入 DATA-342 优化池。

### 📊 数据链路贯通（埋点 → 画像）  
- 小李牵头打通 Kafka → DWD → 画像服务实时通道，首次实现「端上曝光/点击事件 → 实时标签更新」闭环（端到端延迟中位数 1.8s）；  
- 补充校验规则 5 条，拦截异常埋点数据 12.7 万条/日，DWD 层数据质量分达 99.2（+3.1）。

### 🛡️ 运维可观测性升级  
- 小赵交付的告警看板已接入公司统一告警中心（企业微信+电话双通道），8 条规则覆盖：服务健康度、SLA 波动、特征新鲜度、Kafka Lag 突增等关键维度；  
- Dashboard 中新增「灰度流量对比视图」，支持按地域/设备类型下钻分析，刘总周五已用该视图快速定位某安卓渠道标签缺失问题 👏

### 📈 A/B 实验支持  
- 小周高效交付 3 份对外汇报报表（含「新老画像策略转化率对比」「高价值用户召回提升归因」「冷启动用户标签覆盖率趋势」），均通过法务与合规初审；  
- 报表底层逻辑已沉淀为可复用的 SQL 模板（`ab_test_profile_summary_v1`），后续实验开箱即用。

---

## 📋 关键指标概览  

| 模块 | 亮点 | 风险 | 下周计划 |
|------|------|------|-----------|
| **用户画像服务** | ✅ 灰度发布成功，P99 延迟 318ms，错误率 0.017%<br>✅ 自动回滚脚本实测平均恢复时间 42s | ⚠️ 少量安卓设备 ID 归因不一致（约 0.3% 流量），需协同终端 SDK 升级 | ▶️ 启动第二阶段灰度（扩至 25% 流量）<br>▶️ 推进 DATA-342 归因逻辑优化开发 |
| **埋点数据链路** | ✅ 实时链路端到端延迟 ≤2s（达标）<br>✅ DWD 层数据质量分 99.2（历史最高） | ⚠️ iOS 17.4 新版本埋点字段兼容性待验证（预计下周发布） | ▶️ 完成 iOS 兼容性预检清单<br>▶️ 输出《埋点接入自查手册 V1.2》 |
| **可观测性建设** | ✅ 8 条告警规则全部生效，首周触发 0 次误报<br>✅ Dashboard 日均访问 17+ 次（含产品/算法团队） | ⚠️ 告警分级策略待细化（当前均为 P1，需区分业务影响等级） | ▶️ 与 SRE 团队对齐告警分级标准<br>▶️ 上线「告警影响范围评估」辅助字段 |
| **A/B 实验支持** | ✅ 3 份报表按时交付，获产品团队书面致谢<br>✅ 模板化能力已复用至 2 个新实验 | ⚠️ 报表权限模型较粗粒度，暂不支持按业务线隔离 | ▶️ 启动 RBAC 权限改造（DATA-348）<br>▶️ 支持「实验-报表」一键关联导出 |

---

## 🌈 一点小温度  
这周看到小周在日报里写：“报表里的每一条曲线，背后都是真实用户的一次犹豫、一次点击、一次信任。” —— 很喜欢这句话。我们写的不是代码，是用户旅程的注脚；我们调的不是参数，是业务心跳的节拍器。感谢每一位伙伴的专注与温度，下周继续并肩，稳中求进 🚀  

**数据智能平台组 · 2025年3月16日**  
（撰稿：@组长｜审核：全体核心成员）
```
 */
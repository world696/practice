/**
 * 【26】
 * @param {number[]} nums
 * @return {number}
 * 【数组】去重
 * 有个地方需要注意一下，是原地操作，相当于不创建新的内存去存储，直接修改原数组
 * 
 */
const removeDuplicates = function(nums) {
  
}

console.log(removeDuplicates([1,1,2]));
console.log(removeDuplicates([0,0,1,1,1,2,2,3,3,4]));
